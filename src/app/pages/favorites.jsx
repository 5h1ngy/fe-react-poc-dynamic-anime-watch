import Favorites from "app/containers/FavoritesContainer";

const route = {
    id: "favorites",
    path: 'favorites',
    element: <Favorites />,
    errorElement: <></>,
}

export default route;
